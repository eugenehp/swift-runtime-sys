[package]
name = "apple-fskit"
description = "Apple FSKit — file system extensions from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "fskit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
