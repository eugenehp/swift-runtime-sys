[package]
name = "permissionkit"
description = "Apple PermissionKit — permission management from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "permissionkit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
