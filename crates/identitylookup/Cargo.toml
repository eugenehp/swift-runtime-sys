[package]
name = "identitylookup"
description = "Apple IdentityLookup — caller ID and message filtering from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "identitylookup"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
