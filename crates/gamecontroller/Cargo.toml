[package]
name = "gamecontroller"
description = "Apple GameController — controller input from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "gamecontroller"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
