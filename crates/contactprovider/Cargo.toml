[package]
name = "contactprovider"
description = "Apple ContactProvider — contact provider extensions from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "contactprovider"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
