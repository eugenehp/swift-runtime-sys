[package]
name = "rswift-metrickit"
description = "Alias for metrickit — re-exports the metrickit crate under the rswift- namespace"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "rswift_metrickit"

[dependencies]
metrickit = { path = "../metrickit" }

[lints]
workspace = true
