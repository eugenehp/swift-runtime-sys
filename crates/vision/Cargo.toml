[package]
name = "rswift-vision"
description = "Apple Vision framework — image analysis and computer vision from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "vision"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
