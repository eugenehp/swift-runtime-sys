[package]
name = "spritekit"
description = "Apple SpriteKit — 2D game engine from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "spritekit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
