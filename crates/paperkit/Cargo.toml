[package]
name = "paperkit"
description = "Apple PaperKit — paper detection and interaction from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "paperkit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
