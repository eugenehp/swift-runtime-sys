[package]
name = "rswift-devicecheck"
description = "Apple DeviceCheck — device attestation from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "devicecheck"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
