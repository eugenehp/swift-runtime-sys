[package]
name = "rswift-contacts"
description = "Alias for contacts — re-exports the contacts crate under the rswift- namespace"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "rswift_contacts"

[dependencies]
contacts = { path = "../contacts" }

[lints]
workspace = true
