[package]
name = "rswift-gamesave"
description = "Alias for gamesave — re-exports the gamesave crate under the rswift- namespace"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "rswift_gamesave"

[dependencies]
gamesave = { path = "../gamesave" }

[lints]
workspace = true
