[package]
name = "rswift-spatial"
description = "Apple Spatial framework — 3D math types from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "spatial"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
