[package]
name = "rswift-marketplacekit"
description = "Alias for marketplacekit — re-exports the marketplacekit crate under the rswift- namespace"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "rswift_marketplacekit"

[dependencies]
marketplacekit = { path = "../marketplacekit" }

[lints]
workspace = true
