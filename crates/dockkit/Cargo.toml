[package]
name = "dockkit"
description = "Apple DockKit — motorized stand control from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "dockkit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
