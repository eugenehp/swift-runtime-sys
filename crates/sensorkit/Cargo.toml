[package]
name = "sensorkit"
description = "Apple SensorKit — research sensor data from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "sensorkit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
