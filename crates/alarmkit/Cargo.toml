[package]
name = "alarmkit"
description = "Apple AlarmKit — alarm management from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "alarmkit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
