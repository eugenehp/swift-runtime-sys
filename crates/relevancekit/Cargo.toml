[package]
name = "relevancekit"
description = "Apple RelevanceKit — relevance engine from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "relevancekit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
