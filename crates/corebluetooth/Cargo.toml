[package]
name = "apple-corebluetooth"
description = "Apple Core Bluetooth — BLE from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "corebluetooth"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
