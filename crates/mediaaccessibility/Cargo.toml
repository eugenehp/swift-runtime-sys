[package]
name = "mediaaccessibility"
description = "Apple MediaAccessibility — closed captions and audio descriptions from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "mediaaccessibility"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
