[package]
name = "apple-network"
description = "Apple Network framework — modern networking from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "network"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
