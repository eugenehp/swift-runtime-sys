[package]
name = "apple-localauthentication"
description = "Apple LocalAuthentication — biometric auth from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "localauthentication"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
