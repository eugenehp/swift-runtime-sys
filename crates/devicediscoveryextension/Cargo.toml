[package]
name = "devicediscoveryextension"
description = "Apple DeviceDiscoveryExtension — streaming device discovery from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "devicediscoveryextension"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
