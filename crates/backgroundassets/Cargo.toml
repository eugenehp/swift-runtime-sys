[package]
name = "backgroundassets"
description = "Apple BackgroundAssets — background asset downloads from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "backgroundassets"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
