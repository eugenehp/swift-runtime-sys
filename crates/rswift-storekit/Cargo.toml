[package]
name = "rswift-storekit"
description = "Alias for storekit — re-exports the storekit crate under the rswift- namespace"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "rswift_storekit"

[dependencies]
storekit = { path = "../storekit" }

[lints]
workspace = true
