[package]
name = "uniformtypeidentifiers"
description = "Apple UniformTypeIdentifiers — UTI system from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "uniformtypeidentifiers"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
