[package]
name = "callkit"
description = "Apple CallKit — VoIP call integration from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "callkit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
