[package]
name = "roomplan"
description = "Apple RoomPlan — 3D room scanning with LiDAR from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "roomplan"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
