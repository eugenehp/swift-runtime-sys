[package]
name = "browserkit"
description = "Apple BrowserKit — browser kit from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "browserkit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
