[package]
name = "managedappdistribution"
description = "Apple ManagedAppDistribution — enterprise app distribution from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "managedappdistribution"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
