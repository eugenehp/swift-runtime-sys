[package]
name = "messageui"
description = "Apple MessageUI — in-app email and SMS compose from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "messageui"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
