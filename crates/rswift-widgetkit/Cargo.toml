[package]
name = "rswift-widgetkit"
description = "Alias for widgetkit — re-exports the widgetkit crate under the rswift- namespace"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "rswift_widgetkit"

[dependencies]
widgetkit = { path = "../widgetkit" }

[lints]
workspace = true
