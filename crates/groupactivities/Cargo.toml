[package]
name = "groupactivities"
description = "Apple GroupActivities — SharePlay from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "groupactivities"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
