[package]
name = "storekit"
description = "Apple StoreKit — in-app purchases and subscriptions from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "storekit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
