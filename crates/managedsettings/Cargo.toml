[package]
name = "managedsettings"
description = "Apple ManagedSettings — device restrictions from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "managedsettings"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
