[package]
name = "rswift-musickit"
description = "Alias for musickit — re-exports the musickit crate under the rswift- namespace"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "rswift_musickit"

[dependencies]
musickit = { path = "../musickit" }

[lints]
workspace = true
