[package]
name = "ituneslibrary"
description = "Apple iTunesLibrary — Music library access from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "ituneslibrary"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
