[package]
name = "carkey"
description = "Apple CarKey — digital car keys from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "carkey"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
