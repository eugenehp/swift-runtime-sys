[package]
name = "adsupport"
description = "Apple AdSupport — advertising identifier from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "adsupport"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
