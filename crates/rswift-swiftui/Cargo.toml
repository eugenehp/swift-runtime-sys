[package]
name = "rswift-swiftui"
description = "Alias for swiftui — re-exports the swiftui crate under the rswift- namespace"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "rswift_swiftui"

[dependencies]
swiftui = { path = "../swiftui" }

[lints]
workspace = true
