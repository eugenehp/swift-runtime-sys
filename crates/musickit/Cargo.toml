[package]
name = "musickit"
description = "Apple MusicKit — Apple Music integration from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "musickit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
