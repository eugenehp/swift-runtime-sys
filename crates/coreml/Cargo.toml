[package]
name = "rswift-coreml"
description = "Apple Core ML — on-device machine learning from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "coreml"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
