[package]
name = "telephonymessagingkit"
description = "Apple TelephonyMessagingKit — telephony messaging from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "telephonymessagingkit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
