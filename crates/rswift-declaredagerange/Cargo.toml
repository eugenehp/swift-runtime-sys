[package]
name = "rswift-declaredagerange"
description = "Alias for declaredagerange — re-exports the declaredagerange crate under the rswift- namespace"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "rswift_declaredagerange"

[dependencies]
declaredagerange = { path = "../declaredagerange" }

[lints]
workspace = true
