[package]
name = "replaykit"
description = "Apple ReplayKit — screen recording and broadcasting from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "replaykit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
