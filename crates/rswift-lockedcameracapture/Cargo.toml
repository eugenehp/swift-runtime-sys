[package]
name = "rswift-lockedcameracapture"
description = "Alias for lockedcameracapture — re-exports the lockedcameracapture crate under the rswift- namespace"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "rswift_lockedcameracapture"

[dependencies]
lockedcameracapture = { path = "../lockedcameracapture" }

[lints]
workspace = true
