[package]
name = "mediaextension"
description = "Apple MediaExtension — media codec extensions from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "mediaextension"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
