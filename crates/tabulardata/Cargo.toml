[package]
name = "tabulardata"
description = "Apple TabularData — data tables and CSV from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "tabulardata"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
