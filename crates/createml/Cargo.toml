[package]
name = "createml"
description = "Apple Create ML — train machine learning models from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "createml"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
