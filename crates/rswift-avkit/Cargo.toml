[package]
name = "rswift-avkit"
description = "Alias for avkit — re-exports the avkit crate under the rswift- namespace"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "rswift_avkit"

[dependencies]
avkit = { path = "../avkit" }

[lints]
workspace = true
