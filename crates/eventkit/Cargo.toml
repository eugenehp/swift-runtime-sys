[package]
name = "apple-eventkit"
description = "Apple EventKit — calendar and reminders from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "eventkit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
