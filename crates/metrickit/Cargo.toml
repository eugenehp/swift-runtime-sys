[package]
name = "metrickit"
description = "Apple MetricKit — app diagnostics from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "metrickit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
