[package]
name = "rswift-paperkit"
description = "Alias for paperkit — re-exports the paperkit crate under the rswift- namespace"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "rswift_paperkit"

[dependencies]
paperkit = { path = "../paperkit" }

[lints]
workspace = true
