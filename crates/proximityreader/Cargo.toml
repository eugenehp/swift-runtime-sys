[package]
name = "proximityreader"
description = "Apple ProximityReader — Tap to Pay on iPhone from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "proximityreader"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
