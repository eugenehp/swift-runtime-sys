[package]
name = "apple-virtualization"
description = "Apple Virtualization — virtual machines from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "virtualization"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
