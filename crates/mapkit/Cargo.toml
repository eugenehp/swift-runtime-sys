[package]
name = "mapkit"
description = "Apple MapKit — maps and directions from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "mapkit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
