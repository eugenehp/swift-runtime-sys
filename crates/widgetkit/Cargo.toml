[package]
name = "widgetkit"
description = "Apple WidgetKit — reload widget timelines from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "widgetkit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
