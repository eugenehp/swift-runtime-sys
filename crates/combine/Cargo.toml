[package]
name = "rswift-combine"
description = "Combine publisher/subscriber bridge from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "combine_rs"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
