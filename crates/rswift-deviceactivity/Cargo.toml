[package]
name = "rswift-deviceactivity"
description = "Alias for deviceactivity — re-exports the deviceactivity crate under the rswift- namespace"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "rswift_deviceactivity"

[dependencies]
deviceactivity = { path = "../deviceactivity" }

[lints]
workspace = true
