[package]
name = "stickerkit"
description = "Apple StickerKit — iMessage sticker packs from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "stickerkit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
