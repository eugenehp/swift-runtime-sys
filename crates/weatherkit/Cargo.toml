[package]
name = "rswift-weatherkit"
description = "Apple WeatherKit — weather data from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "weatherkit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
