[package]
name = "networkextension"
description = "Apple NetworkExtension — VPN and content filtering from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "networkextension"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
