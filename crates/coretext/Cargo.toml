[package]
name = "coretext"
description = "Apple Core Text — text layout and font handling from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "coretext"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
