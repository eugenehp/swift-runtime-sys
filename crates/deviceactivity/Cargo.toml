[package]
name = "deviceactivity"
description = "Apple DeviceActivity — Screen Time monitoring from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "deviceactivity"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
