[package]
name = "nearbyinteraction"
description = "Apple NearbyInteraction — UWB ranging from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "nearbyinteraction"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
