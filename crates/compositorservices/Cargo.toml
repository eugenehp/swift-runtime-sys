[package]
name = "compositorservices"
description = "Apple CompositorServices — visionOS rendering from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "compositorservices"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
