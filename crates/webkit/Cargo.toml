[package]
name = "webkit"
description = "Apple WebKit — web views from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "webkit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
