[package]
name = "apple-symbols"
description = "Apple Symbols — SF Symbols metadata from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "symbols"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
