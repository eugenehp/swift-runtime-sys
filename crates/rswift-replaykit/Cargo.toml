[package]
name = "rswift-replaykit"
description = "Alias for replaykit — re-exports the replaykit crate under the rswift- namespace"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "rswift_replaykit"

[dependencies]
replaykit = { path = "../replaykit" }

[lints]
workspace = true
