[package]
name = "rswift-gamekit"
description = "Apple GameKit — Game Center from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "gamekit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
