[package]
name = "cryptokit"
description = "Apple CryptoKit — cryptography from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "cryptokit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
