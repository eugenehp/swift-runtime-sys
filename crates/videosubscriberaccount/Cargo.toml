[package]
name = "videosubscriberaccount"
description = "Apple VideoSubscriberAccount — TV provider authentication from Rust"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "videosubscriberaccount"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
